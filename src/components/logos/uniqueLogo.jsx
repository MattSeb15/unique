function UniqueLogo ({ width, height, className }) {
  return <img src='./src/assets/images/logo.avif' width={width} height={height}  className= {className}alt='Unique Logo' />
}

export default UniqueLogo
